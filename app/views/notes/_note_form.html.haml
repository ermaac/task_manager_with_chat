= form_for @note, url: list_notes_path(list, @note) do |f|
  .form-group
    = f.text_area :text, class: 'form-control', required: true, placeholder: "New note", size: 'auto x2'
    = f.submit @form_name, class: 'btn btn-success btn-block', disabled: list.is_disabled
