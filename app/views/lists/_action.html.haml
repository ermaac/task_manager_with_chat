%li
  = link_to "#{action.link_path}", method: "#{action.method}", title: "#{action.hint}", 'data-toggle': "#{action.data_toggle}", remote: "#{action.remote}" do
    %div{class: "#{action.icon_class}"}
