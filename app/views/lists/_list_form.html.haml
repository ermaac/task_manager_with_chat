.col-md-8
  = form_for @list, url: board_lists_path(@board, @list) do |f|
    .form-group
      = f.text_field :title, class: 'form-control', required: true, placeholder: "List's title", autocomplete: 'off'
      = f.submit @form_name, class: 'btn btn-success btn-block'
